This target ships with a real off-by-one bug (`result = 1` instead of `0` in `total`), so the goal-bridge demo starts red: the unittest suite fails on camera, the agent derives the Goal-Bridge contract first (metric, threshold, evidence, rollback), then fixes `counter.py` and drives the metric to exit 0, appending proof to the evidence file each round until the contract is met (green).
